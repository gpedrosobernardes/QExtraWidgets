site_name: QExtraWidgets
theme:
  name: material

plugins:
  - search
  - include-markdown
  - mkdocstrings:
      default_handler: python
      handlers:
        python:
          paths: [source]

nav:
  - Home: index.md
  - Referência da API: api.md